---
title: "world.manifest"
description: "Re-fetch the current world manifest. Agent → World."
---

No params. Returns the same document as `initialize`. The manifest is stable for the life of the connection; a change requires a new `initialize` (AWP-MAN-003).
