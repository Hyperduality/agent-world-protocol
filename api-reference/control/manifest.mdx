---
title: "world.manifest"
description: "Re-fetch the current world manifest. Agent → World."
---

No params. Returns the same document as `initialize`. Useful after reconnect; the manifest is stable per connection (AWP-MAN-003), so a changed manifest implies the world restarted.
