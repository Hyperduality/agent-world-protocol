{
  "name": "agent-world-protocol-docs",
  "private": true,
  "version": "0.1.0",
  "description": "Agent World Protocol documentation, canonical JSON Schemas, and validation tooling.",
  "license": "Apache-2.0",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "gen:schema-docs": "node scripts/gen-schema-docs.mjs",
    "gen:types": "node scripts/gen-types.mjs",
    "gen:requirements": "node scripts/gen-requirements.mjs",
    "gen:traces": "node scripts/gen-traces.mjs",
    "gen": "npm run gen:schema-docs && npm run gen:types && npm run gen:requirements && npm run gen:traces",
    "check": "npm run validate && npm run gen:schema-docs -- --check && npm run gen:types -- --check && npm run gen:requirements -- --check && npm run gen:traces -- --check"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "json-schema-to-typescript": "^15.0.4",
    "yaml": "^2.6.1"
  }
}
